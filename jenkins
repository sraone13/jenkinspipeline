pipeline {
    agent any

    stages {
        stage('build') {
            steps {
                echo 'build is done'
            }
        }
        stage('test') {
            steps {
                echo 'testing is done'
            }
        }
        stage('deploy') {
            steps {
                echo 'this is for deployment'
            }
        }
    }
}
